import { Space } from "antd";
import "./globals.css";

// Routing from middleware
// Because the route is /
// we need the route to be /dashboard

const App = () => {
  return <Space direction="vertical" style={{ width: "100%" }}></Space>;
};

export default App;
